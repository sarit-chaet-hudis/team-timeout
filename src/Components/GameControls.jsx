import Score from "./Score";
import Timer from "./Timer";

const GameControls = () => {
  return (
    <div className="GameControls">
      <Score />
      <Timer />
    </div>
  );
};

export default GameControls;
